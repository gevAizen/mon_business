## Users Request

- [ ] Set the entries one by one is too much (one client can buy several things). Let's have a "transaction" contains several entries, so they can select product and quantity (total price automatic) and a final submit to handle them all
- [ ] Add these in expenses: "electric bill", "tontine (epargne)"
- [ ] Product name should be unique (stock management page)
